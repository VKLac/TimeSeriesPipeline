version: "3.9"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: vilacerda
      POSTGRES_PASSWORD: aiops123
      POSTGRES_DB: monitoramento
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
